Drop a Gateway map image here named `gateway-map.jpg` to use it as the live map
background. Without it the map falls back to a coordinate grid (the app still
works fully). Use a top-down community map of the Isle: Evrima "Gateway" map;
square images line up best with the projection.
